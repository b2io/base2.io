const breakpoints = {
  /* eslint-disable sort-keys */
  sm: "640px",
  md: "768px",
  lg: "1024px",
  xl: "1280px",
  "2xl": "1536px",
  /* eslint-enable sort-keys */
};

const mediaQueries = {
  /* eslint-disable sort-keys */
  sm: `(min-width: 640px)`,
  md: `(min-width: 768px)`,
  lg: `(min-width: 1024px)`,
  xl: `(min-width: 1280px)`,
  "2xl": `(min-width: 1536px)`,
  /* eslint-enable sort-keys */
};

export { breakpoints, mediaQueries };
