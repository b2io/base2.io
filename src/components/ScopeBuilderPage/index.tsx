import type { FC } from 'react';

import { Layout } from '~/components';

export const ScopeBuilderPage: FC = () => {
  // Add description
  return (
    <Layout description="" title="ScopeBuilder">
      <h1>hello from the scope builder page</h1>
    </Layout>
  );
};
