import type { NextPage } from 'next';

import { ScopeBuilderPage } from '~/components';

const ScopeBuilderNextPage: NextPage = () => {
  return <ScopeBuilderPage />;
};

export default ScopeBuilderNextPage;
