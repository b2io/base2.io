import type { FC } from 'react';

import { Layout } from '~/components';

export const NetJetsPage: FC = () => {
  // Add description for NetJetsPage.
  return (
    <Layout description="" title="NetJets">
      <h1>hello from the netjets page</h1>
    </Layout>
  );
};
