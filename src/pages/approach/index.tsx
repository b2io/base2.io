import { NextPage } from 'next';

import { Layout } from '~/components';

const ApproachIndexPage: NextPage = () => {
  return (
    <Layout>
      <h1>Approach challenges require unique solutions.</h1>
    </Layout>
  );
};

export default ApproachIndexPage;
