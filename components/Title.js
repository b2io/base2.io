import styled from 'styled-components';

const Title = styled.h1`
  color: var(--color-fg, black);
  display: block;
  font-family: var(--font, sans-serif);
  font-size: calc(2.5vw + 5vmin);
  font-weight: 100;
  text-transform: none;
  width: 100%;
`;

export default Title;
