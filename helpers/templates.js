import { css } from 'styled-components';

const fullWidth = css`
  --font: 'Roboto';
  max-width: 100%;
  overflow: hidden;
`;

const home = css`
  --color-fg: white;
  --color-bg: black;
`;

export { fullWidth, home };
